# 単語学習アプリ (AsuMe) - 開発ガイドライン

## 実行・ビルド・テストのコマンド
- 開発サーバー起動: `npm run dev`
- ビルド: `npm run build`
- テスト実行: `npm run test` (Jest & React Testing Library を使用)

## 1. アーキテクチャとデータ構造
- **フロントエンドのみ**: Next.js (App Router) を使用。状態管理とデータ保存はすべてブラウザの `localStorage` (`asume_projects` キー) で完結しています。サーバーサイドのロジックや外部データベースは持ちません。
- **データモデル (`src/types/index.ts`)**:
  - `QuestionData`: 単語ID, 英単語, 日本語訳, 発音記号, 穴埋め用英文, 品詞(`partOfSpeech`)
  - `Project`: 複数の `QuestionData` を保持。`wrongQuestionIds` 配列に間違えた問題のIDを記録し、復習モードに使用します。

## 2. CSVフォーマット仕様
アップロードされるCSVの1行目（ヘッダー）は、以下の列名を含んでいる必要があります。
- `paragraphs`: 穴埋め問題用のコンテキストとなる英文（例: "The level of _."）。空欄箇所はアンダースコア `_` で表記。
- `words`: 正解となる英単語（例: "ambiguity"）
- `japanese`: その英単語の日本語訳（例: "曖昧さ"）
- `phonetic` (任意): 発音記号（例: "æmbɪˈgjuːəti"）
- `partOfSpeech` (任意): 品詞や変形情報（例: "名詞", "動詞(過去形)"）

## 3. 主要ライブラリ
- **Tailwind CSS v4**: スタイリングに使用。テーマカラーはオレンジ・白・緑で、親しみやすい「AsuMe」UIを構築しています。
- **PapaParse**: ユーザーがアップロードしたCSVファイルを `QuestionData` の配列にパースするために使用 (`src/lib/csvParser.ts`)。
- **JSZip**: プロジェクトのデータや進捗をZIPファイルとしてエクスポート（バックアップ）およびインポート・復元するために使用 (`src/lib/zipManager.ts`)。

## 4. コーディング規約・ルール
- **テスト駆動開発 (TDD)**: ロジックやユーティリティを追加・修正する際は、必ずJestを用いたテストファイル（`*.test.ts`）を作成・更新・パスさせてから実装してください。
- **クライアントコンポーネント**: `localStorage` や `window.speechSynthesis` に依存し、インタラクティブなUIが中心のため、PageやComponentの先頭には `"use client";` を宣言してください。
- **クイズエンジンの仕様**:
  - 4択問題の「不正解のダミー3つ」は、必ず同じプロジェクト（CSV）内に登録されている他の単語データからランダムに抽出して生成します。
  - ランダムに抽出された**最大10問**を1セットとして出題します。
  - 学習中（1セット中）に間違えた問題は、そのセット内で正解するまで繰り返し出題されます（キューの最後に追加されます）。

## 5. CSV Generator (単語データ自動生成機能) の仕様
- ユーザーが「英単語のみ」を入力するだけで、APIを利用して完全な単語データ（プロジェクト）を自動生成します。
- **使用APIと役割**:
  - **WordsAPI**: 単語の定義、例文、品詞、および発音記号（IPA形式）の取得。
  - **cmu-pronouncing-dictionary (npm)**: WordsAPIで発音記号が取れなかった場合のフォールバック（ARPAbet形式・小文字化）。
  - **DeepL API**: 英単語を日本語に翻訳し `japanese` 列を埋める。言語誤検知による珍訳を防ぐため、必ず `source_lang: 'EN'` を指定します。
  - **OpenAI API (gpt-4o-mini)**: WordsAPIから例文が提供されなかった場合、文脈に沿った自然で短い英文を自動生成するための最終フォールバックとして使用します。
- **穴埋め例文（paragraph）の置換ロジック**:
  - 例文中の対象単語を空欄にする際、過去形や進行形などの「活用形（例: attempted）」も検知して置換します。
  - 原形と完全一致した場合は `_` に置換。
  - 活用形として使われている場合は、学習時のヒントとして `_[活用形]` に置換します。

## 6. 次の開発目標 (学習履歴とアルゴリズムの強化)
- **データ構造の拡張**: `QuestionData` に `correctCount`, `incorrectCount`, `consecutiveCorrectCount`, `isMastered`, `lastStudiedAt` を追加。
- **周回数と殿堂入り**: 全単語を「最低1回」解くと1周と定義する。3回連続正解した単語は「殿堂入り（`isMastered: true`）」とし、通常学習には出題されなくなる。
- **出題アルゴリズムの改善**: 殿堂入りを除外した上で、「未回答の単語」や「正答率の低い単語」を優先的に出題する。既存の「復習ボタン」は直近で間違えた単語の特訓用として残す。
- **プロジェクト管理ページ**: `/manage/[projectId]` を新設。成績の確認、単語の個別削除、殿堂入りのリセット、および「新しい単語1件の自動生成・追加機能」を実装する。

## 7. バリデーションと例外処理の仕様
- **CSVアップロード時**: `words` 列が存在しない、または単語が空文字の行は自動的に除外（フィルタリング）されます。有効な単語が1つもない場合はエラーとしてアラートを表示します。
- **クイズの画面遷移**: 1セット（最大10問）を終えた直後の再レンダリング時に発生する配列外参照（Index Out of Bounds）を防ぐため、存在しない問題インデックスを参照した場合は安全に `null` を返し、結果画面へ遷移させます。

## 7. [重要] AIツール利用時の注意事項
- **テンプレートリテラルのエスケープ厳禁**: ファイルの書き込み・置換（`write_to_file` 等）をAIに行わせる際、TypeScriptのテンプレートリテラル内のバッククォート( \` ) や ドル記号 ( \$ ) を**絶対にバックスラッシュ（ \ ）でエスケープしないでください**。ファイル内にそのままバックスラッシュが記述され、Next.jsのSyntax Errorの直接的な原因になります。
- **ターミナル実行時の考慮**: ユーザー環境はWindows (PowerShell) のため、実行ポリシーの影響を受ける場合があります。
